{% extends 'base.html.twig' %}

{% block main %}

    {# This block show the form #}
    <div style="margin: 20px;">
        {{ form_start(our_form) }}
        {{ form_widget(our_form) }}
        {{ form_end(our_form) }}
    </div>

{% endblock %}